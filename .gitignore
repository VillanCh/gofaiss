# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove if you have vendored dependencies)
/vendor/

# Go workspace file
go.work
go.work.sum

# Don't ignore our pre-compiled libs.
# The ! negates a pattern.
!faisslib/**/libfaiss_c.dylib
!faisslib/**/libfaiss.dylib
!faisslib/**/libfaiss_c.so
!faisslib/**/libfaiss.so
